/** Canonical production origin. Use this everywhere a full URL is needed
 * (canonical links, og:url, share links, MCP tool responses) instead of
 * hardcoding a domain — the Lovable staging host has leaked into several
 * places in the past and confuses both SEO signals and shared links. */
export const SITE_URL = "https://playpictaria.com";
